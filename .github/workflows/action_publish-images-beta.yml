name: Docker Publish (Beta Images)
run-name: >-
  Beta images (${{
    github.event.release.tag_name ||
    (github.event_name == 'schedule' && 'weekly rebuild') ||
    'manual rebuild'
  }})

on:
  workflow_dispatch:
  release:
    types: [prereleased]
  schedule:
    - cron: '25 8 * * 1'

permissions: {}

jobs:
  release:
    name: latest beta release
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      tag: ${{ steps.release.outputs.tag }}
    steps:
      - name: Find the latest beta release
        id: release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          TAG=$(gh api "repos/${GH_REPO}/releases?per_page=30" --jq '[.[] | select(.prerelease)][0].tag_name')
          if [ -z "$TAG" ] || [ "$TAG" = "null" ]; then
            echo "::error title=No beta release found::Could not find a prerelease to build."
            exit 1
          fi
          echo "Building beta release $TAG"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

  setup:
    needs: release
    uses: ./.github/workflows/service_setup-matrix.yml
    permissions:
      contents: read
    with:
      ref: ${{ needs.release.outputs.tag }}

  build:
    needs: [release, setup]
    name: ${{ matrix.variation }}
    strategy:
      fail-fast: false
      matrix:
        variation: ${{ fromJson(needs.setup.outputs.variations) }}
    permissions:
      contents: read
      packages: write
      id-token: write
    uses: ./.github/workflows/service_docker-build-and-publish.yml
    with:
      variation: ${{ matrix.variation }}
      matrix: ${{ needs.setup.outputs.matrix }}
      ref: ${{ needs.release.outputs.tag }}
      registry-repositories: "docker.io/serversideup/php,ghcr.io/serversideup/php"
      tag-prefix: "beta"
      release-type: "testing"
    secrets: inherit

  smoke:
    needs: [release, setup, build]
    if: always() && needs.setup.result == 'success'
    uses: ./.github/workflows/service_smoke-test.yml
    permissions:
      contents: read
    with:
      ref: ${{ needs.release.outputs.tag }}

  summary:
    needs: [release, setup, build]
    if: always() && needs.setup.result == 'success'
    uses: ./.github/workflows/service_summary.yml
    permissions:
      contents: read
    with:
      matrix: ${{ needs.setup.outputs.matrix }}
      ref: ${{ needs.release.outputs.tag }}
